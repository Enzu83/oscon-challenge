pub enum INSTRUCTION {
    HALT,
    SET(u16, u16),
    PUSH(u16),
    POP(u16),
    EQ(u16, u16, u16),
    GT(u16, u16, u16),
    JMP(u16),
    JT(u16, u16),
    ADD(u16, u16, u16),
    MULT(u16, u16, u16),
    MOD(u16, u16, u16),
    AND(u16, u16, u16),
    OR(u16, u16, u16),
    NOT(u16, u16),
    RMEM(u16, u16),
    WMEM(u16, u16),
    CALL(u16),
    RET,
    OUT(u16),
    IN(u16),
    NOOP,
}
